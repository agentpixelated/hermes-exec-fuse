name: hermes-exec-fuse
version: "0.2.0"
description: Safely batch foreground terminal commands, reuse exact read-only results, normalize failures, and compact tool output.
author: agentpixelated
provides_tools:
  - exec_fuse
  - exec_fuse_stats
  - exec_fuse_clear_cache
provides_hooks:
  - pre_tool_call
  - post_tool_call
  - pre_llm_call
